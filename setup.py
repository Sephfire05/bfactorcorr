from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Finds and calculates b_factor for every residue.',
    author='UTA CDS Lab',
    license='',
)
